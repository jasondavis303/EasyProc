<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Version>1.0.0.0</Version>
    <Authors>Jason Davis</Authors>
    <Company>jasondavis303</Company>
    <Description>Just a lib to avoid some boilerplate code I have to often write whenever I want to run a command line program and get it's output (like ffmpeg)</Description>
    <Copyright>jasondavis303</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/jasondavis303/EasyProc</PackageProjectUrl>
    <RepositoryUrl>https://github.com/jasondavis303/EasyProc</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

</Project>
